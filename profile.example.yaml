# Profiles now live per Application Track:
#
#   tracks/software-engineering/profile.example.yaml
#   tracks/it-support/profile.example.yaml
#
# Copy the example for your track to profile.yaml in the same folder, then set
# activeTrack in track.yaml (software-engineering | it-support).
